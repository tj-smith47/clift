version: '3'
# Hidden: parses + runs, absent from help + completion.
vars:
  HIDDEN: true
  FLAGS: []

tasks:
  default:
    desc: "Dump internal state (hidden)"
    vars:
      FLAGS: []
    cmd: "CLI_ARGS='{{.CLI_ARGS}}' '{{.FRAMEWORK_DIR}}/lib/router/router.sh' '{{.TASK}}'"

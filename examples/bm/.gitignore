# Generated by clift compile.sh / setup:cli.
/.clift/tasks.json
/.clift/flags.json
/.clift/index.json
/.clift/checksum
/.clift/sources

# Wrapper script — generated by `clift setup:cli`. Each install resolves
# absolute paths for the host, so it's not a portable artifact to commit.
/bin/bm

# go-task checksum cache.
/.task/

name: clift
version: 0.1.0
description: Batteries-included framework for building custom CLIs with go-task
min_task_version: "3.0.0"

# Dependencies listed here are informational — read by humans and external tools.
# deps.sh handles framework dep checks directly.
dependencies:
  required:
    - jq
    - yq
  optional:
    - gum
